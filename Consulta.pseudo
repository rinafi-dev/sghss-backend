classe Consulta:
    atributos:
        id: inteiro
        pacienteId: inteiro
        medicoId: inteiro
        dataHora: data/hora
        tipo: texto
        status: texto
    métodos:
        agendar()
        cancelar()
